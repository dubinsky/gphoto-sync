pluginManagement {
  repositories {
    mavenLocal()
    mavenCentral()
    gradlePluginPortal()  // for JIB
  }
}

dependencyResolutionManagement {
  repositories {
    mavenLocal()
    mavenCentral()
  }
}

rootProject.name = 'org.podval.tools.photo'
